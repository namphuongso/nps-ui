export interface VersionConfig {
  version: string;
  label: string;
  date: string;
}

export const CURRENT_VERSION: VersionConfig = {
  version: "0.1.0",
  label: "v0.1.0",
  date: "2026-04-14",
};
